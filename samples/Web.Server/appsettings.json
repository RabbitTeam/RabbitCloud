{
  "ServiceHost": "localhost",
  "ConsulHost": "192.168.1.150",
  "ServicePort": 5000,
  "RabbitCloud": {
    "Consul": {
      "Address": "http://${ConsulHost}:8500/",
      "Discovery": {
        "InstanceId": "UserService_${ServiceHost}_${ServicePort}",
        "ServiceName": "UserService",
        "HostName": "${ServiceHost}",
        "Port": "${ServicePort}",
        "HealthCheckInterval": "10s",
        "Tags": [
          "samples"
        ]
      }
    }
  }
}