{
  "ServiceHost": "192.168.1.100",
  "ConsulHost": "192.168.1.100",
  "ServicePort": 5000,
  "RabbitCloud": {
    "Consul": {
      "Address": "http://${ConsulHost}:8500/",
      "Discovery": {
        "InstanceId": "UserService_localhost",
        "ServiceName": "UserService",
        "HostName": "${ServiceHost}",
        "Port": "${ServicePort}",
        "HealthChecks": [
          {
            "Url": "http://${ServiceHost}:${ServicePort}/User/GetUser",
            "Interval": "10s"
          }
        ],
        "HealthCheckUrl": "http://${ServiceHost}:${ServicePort}/health",
        "HealthCheckInterval": "10s",
        "Tags": [
          "examples"
        ]
      }
    }
  }
}